# Experiment input package

This directory is `INPUT_ROOT`.

It is portable. Copy `01_Input_Files/`, `02_Implementation/` and
`03_Run-Statistics/` together into any project. Path names are defined
in `03_Process/RUN_INSTRUCTIONS.md`.

Do not assume a repository name or an absolute filesystem path.
