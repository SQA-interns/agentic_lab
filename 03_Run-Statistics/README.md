# Run statistics

This directory is `STATISTICS_ROOT`.

At run start copy `03_Process/run-log.template.json` from `INPUT_ROOT`
to `run-log.json` here.

Do not invent measurements. At the end of the run also write
`run-summary.md`.
